numpy
pandas
openpyxl

# The estimating engine, the WBS roll-up and the workbook writers all
# live in cost_core now, so the tool does not start without it. It is on
# PyPI as cost-core, and this pins the exact release the window was written
# and tested against, because every number in a workbook comes from it.
# lot_cost_model.py repeats the version in the hint it prints when the
# library is missing or on the wrong major version, so a bump touches both.
cost-core==2.1.0

# scipy comes in with cost_core rather than being asked for here. It is
# what supplies the t, chi-square and normal quantiles the intervals and
# the Monte Carlo are built on.
