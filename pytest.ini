[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    # cost_core warns loudly on a short lot series. That is the correct
    # behaviour and one test asserts it happens, so do not treat it as an error.
    default
